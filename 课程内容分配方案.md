# 课程内容分配方案

## 一、分配原则

根据每门课程的 `requiredTimes`（需要学习次数），合理分配：
- **课程导论**：第1次上课时出现
- **课堂互动**：第2次或中间某次上课时出现（仅出现一次）
- **期末考试**：最后一次上课时出现（完成 requiredTimes 后）

**重要**：**每门课的导论播放完成后，立即播放该课的课堂互动**（如果有），而不是先播放所有导论再播放所有互动。

---

## 二、详细分配表

### 2.1 requiredTimes = 1 的课程（单次课程）

| 课程ID | 课程名称 | 第1次 | 说明 |
|--------|---------|-------|------|
| p3 | 时事政治报告 | 导论 | 有导论，无互动，无考试题目 |
| p4 | 三民主义研究 | 导论 | 有导论，无互动，无考试题目 |
| p8 | 世界革命运动史 | 导论 + 期末考试 | 有导论，无互动，有考试题目（4道） |
| p11 | 科学社会主义 | 导论 + 期末考试 | 有导论，无互动，有考试题目（2道） |
| p12 | 国外时事和形势 | 导论 + 期末考试 | 有导论，无互动，有考试题目（5道） |
| m2 | 民众运动 | 导论 + 互动 + 期末考试 | 有导论，有互动，有考试题目（5道） |
| m3 | 政府工作 | 导论 + 期末考试 | 有导论，无互动，有考试题目（5道） |
| l1 | 建校劳动 | 导论 | 劳动课程，无考试题目 |
| l2 | 农业生产劳动 | 导论 | 劳动课程，无考试题目 |

**说明**：
- p3、p4：只有导论，无互动和考试
- p8、p11、p12、m3：有导论和考试，无互动
- m2：有导论、互动和考试（单次课程但内容丰富）
- l1、l2：劳动课程，只有导论，无考试

---

### 2.2 requiredTimes = 2 的课程（两次课程）

| 课程ID | 课程名称 | 第1次 | 第2次 | 说明 |
|--------|---------|-------|-------|------|
| p1 | 社会科学概论 | 导论 | 互动 | 有导论，有互动（多段），无考试题目 |
| p2 | 中国革命运动史 | 导论 | 互动 | 有导论，有互动（多段+选择题），无考试题目 |
| p5 | 抗日民族统一战线 | 导论 | 互动 | 有导论，有互动（多段+选择题），无考试题目 |
| p6 | 政治经济学 | 导论+互动 | 期末考试 | 有导论，有互动，有考试题目（3道） |
| p7 | 辩证唯物主义 | 导论+互动  | 期末考试 | 有导论，有互动，有考试题目（3道） |
| p14 | 党课 | 导论 | 互动 + 期末考试 | 有导论，有互动，有考试题目（5道） |
| m1 | 劳动实践理论 | 导论+互动 | 期末考试 | 有导论，有互动，有考试题目（5道） |


**说明**：
- p1、p2、p5：有导论和互动，但无考试题目（互动内容丰富）
- p6、p7、p14、m1：有导论、互动和考试题目
- mi2、mi5：题目文件中无内容，跳过

---

### 2.3 requiredTimes = 3 的课程（三次课程）

| 课程ID | 课程名称 | 第1次 | 第2次 | 第3次 | 说明 |
|--------|---------|-------|-------|-------|------|
| p10 | 马克思列宁主义 | 导论 | 互动 | 期末考试 | 有导论，有互动（多段），有考试题目（4道） |


**说明**：
- p10：有导论、互动和考试题目，第1次导论，第2次互动，第3次考试
- mi1、mi3、mi4：题目文件中无内容，跳过

---

## 三、分配逻辑总结

### 3.1 通用规则

1. **课程导论**：始终在第1次上课时出现
2. **课堂互动**：
   - 如果课程有互动内容且 requiredTimes ≥ 2，在第2次上课时出现
   - 如果课程有互动内容且 requiredTimes = 1，可以在第1次与导论一起出现
   - 如果课程无互动内容，跳过此环节
3. **期末考试**：始终在最后一次上课时出现（完成 requiredTimes 后）

### 3.2 特殊情况处理

- **劳动课程（l1, l2）**：只有导论，无期末考试（劳动课程可以无限次学习）
- **单次课程但有互动内容（m2）**：导论 + 互动 + 期末考试都在第1次出现
- **无互动内容的课程**：只有导论和期末考试，中间次数为常规学习

---

## 四、实现数据结构

### 4.1 课程内容配置

```javascript
// 课程内容分配配置（根据课程游戏题目.md实际内容）
const courseContentSchedule = {
    // requiredTimes = 1 的课程
    p3: { intro: 1, scene: null, quiz: null },  // 只有导论，无互动和考试
    p4: { intro: 1, scene: null, quiz: null },  // 只有导论，无互动和考试
    p8: { intro: 1, scene: null, quiz: 1 },     // 有导论和考试（4道题），无互动
    p11: { intro: 1, scene: null, quiz: 1 },     // 有导论和考试（2道题），无互动
    p12: { intro: 1, scene: null, quiz: 1 },    // 有导论和考试（5道题），无互动
    m2: { intro: 1, scene: 1, quiz: 1 },        // 有导论、互动和考试（5道题）
    m3: { intro: 1, scene: null, quiz: 1 },     // 有导论和考试（5道题），无互动
    l1: { intro: 1, scene: null, quiz: null },  // 劳动课程，只有导论
    l2: { intro: 1, scene: null, quiz: null },   // 劳动课程，只有导论
    
    // requiredTimes = 2 的课程
    p1: { intro: 1, scene: 2, quiz: null },     // 有导论和互动（多段），无考试题目
    p2: { intro: 1, scene: 2, quiz: null },     // 有导论和互动（多段+选择题），无考试题目
    p5: { intro: 1, scene: 2, quiz: null },     // 有导论和互动（多段+选择题），无考试题目
    p6: { intro: 1, scene: 2, quiz: 2 },         // 有导论、互动和考试（3道题）
    p7: { intro: 1, scene: 2, quiz: 2 },         // 有导论、互动和考试（3道题）
    p14: { intro: 1, scene: 2, quiz: 2 },       // 有导论、互动和考试（5道题）
    m1: { intro: 1, scene: 2, quiz: 2 },       // 有导论、互动和考试（5道题）
    mi2: { intro: null, scene: null, quiz: null }, // 题目文件中无内容
    mi5: { intro: null, scene: null, quiz: null }, // 题目文件中无内容
    
    // requiredTimes = 3 的课程
    p10: { intro: 1, scene: 2, quiz: 3 },        // 有导论、互动（多段）和考试（4道题）
    mi1: { intro: null, scene: null, quiz: null }, // 题目文件中无内容
    mi3: { intro: null, scene: null, quiz: null }, // 题目文件中无内容
    mi4: { intro: null, scene: null, quiz: null }  // 题目文件中无内容
};
```

### 4.2 判断逻辑

```javascript
// 判断当前学习次数应该显示什么内容
function getCourseContentForProgress(courseId, currentProgress, requiredTimes) {
    const schedule = courseContentSchedule[courseId];
    if (!schedule) return { intro: false, scene: false, quiz: false };
    
    const result = {
        intro: schedule.intro === currentProgress,
        scene: schedule.scene === currentProgress,
        quiz: schedule.quiz === currentProgress && currentProgress >= requiredTimes
    };
    
    return result;
}
```

---

## 五、完整流程示例

### 5.1 示例1：p1 社会科学概论（requiredTimes = 2，有导论和互动，无考试题目）

**第1次选择 p1**：
1. 用户选择 p1 → 点击确认本周课程
2. 进入周结算前检查：`currentProgress = 0, requiredTimes = 2`
3. 判断：`intro: 1 === 0 + 1` → 显示课程导论弹窗
4. 点击"开始学习" → **立即显示该课的课堂互动弹窗**（因为 `scene: 2` 但这是第1次，不显示）
5. 实际上：第1次只显示导论，点击"开始学习"后进入周结算
6. 更新进度：`courseProgress.p1 = 1`

**第2次选择 p1**：
1. 用户选择 p1 → 点击确认本周课程
2. 进入周结算前检查：`currentProgress = 1, requiredTimes = 2`
3. 判断：
   - `intro: 1 === 1 + 1` → false（已播放过）
   - `scene: 2 === 1 + 1` → true（第2次，显示互动）
   - `quiz: null` → false（无考试题目）
4. **直接显示课堂互动弹窗**（多段互动）
5. 点击"继续" → 进入周结算（正常学习，获得属性加成）
6. 更新进度：`courseProgress.p1 = 2`
7. 周结算后，因为 `currentProgress >= requiredTimes`，但无考试题目，直接显示周总结

**注意**：如果第1次选择时既有导论又有互动（如m2），则流程为：导论 → 点击"开始学习" → 立即显示互动 → 点击"继续" → 结算

### 5.2 示例2：p10 马克思列宁主义（requiredTimes = 3，有导论、互动和考试）

**第1次选择 p10**：
1. 显示：课程导论弹窗
2. 点击"开始学习" → 进入周结算
3. 更新进度：`courseProgress.p10 = 1`

**第2次选择 p10**：
1. 显示：课堂互动弹窗（多段互动）
2. 点击"继续" → 进入周结算
3. 更新进度：`courseProgress.p10 = 2`

**第3次选择 p10**：
1. 显示：无特殊内容（常规学习）
2. 进入周结算
3. 更新进度：`courseProgress.p10 = 3`
4. 周结算后：因为 `currentProgress >= requiredTimes` 且 `quiz: 3 === 3`，显示期末考试弹窗（4道题）

### 5.3 示例3：p3 时事政治报告（requiredTimes = 1，只有导论，无互动和考试）

**第1次选择 p3**：
1. 显示：课程导论弹窗
2. 点击"开始学习" → 进入周结算（正常学习）
3. 更新进度：`courseProgress.p3 = 1`
4. 周结算后：因为 `currentProgress >= requiredTimes`，但无考试题目，直接显示周总结

### 5.4 示例4：m2 民众运动（requiredTimes = 1，有导论、互动和考试）

**第1次选择 m2**：
1. 显示：课程导论弹窗
2. 点击"开始学习" → **立即显示该课的课堂互动弹窗**（因为 `scene: 1 === 0 + 1`）
3. 点击"继续" → 进入周结算（正常学习）
4. 更新进度：`courseProgress.m2 = 1`
5. 周结算后：因为 `currentProgress >= requiredTimes` 且 `quiz: 1 === 1`，显示期末考试弹窗（5道题）

**这是典型的"导论 → 互动"连续播放示例**

---

## 六、游戏状态扩展

```javascript
gameState = {
    // ... 现有字段
    courseProgress: {},           // 记录每门课程的学习进度
    shownCourseIntroIds: {},      // 记录每门课程是否已播放导论 { courseId: true/false }
    shownCourseSceneIds: {},      // 记录每门课程是否已播放互动 { courseId: true/false }
    
    // 本周待播放的内容队列
    courseIntroQueue: [],         // [{ id, name, progress }]
    courseSceneQueue: [],         // [{ id, name, progress }]
    courseQuizQueue: []           // [{ id, name }] - 在周结算后处理
};
```

---

## 七、实施要点

### 7.1 confirmWeek() 函数改造

```javascript
function confirmWeek() {
    if (gameState.selectedItems.length === 0) return;
    
    const courses = gameState.selectedItems.filter(item => item.itemType === 'course');
    
    // 构建课程内容队列（每门课程按顺序：导论 → 互动）
    gameState.courseContentQueue = courses
        .map(course => {
            const currentProgress = gameState.courseProgress[course.id] || 0;
            const schedule = courseContentSchedule[course.id];
            if (!schedule) return null;
            
            const item = {
                id: course.id,
                name: course.name,
                showIntro: schedule.intro === currentProgress + 1 && 
                          !gameState.shownCourseIntroIds[course.id],
                showScene: schedule.scene === currentProgress + 1 && 
                          !gameState.shownCourseSceneIds[course.id]
            };
            
            // 如果这门课需要显示导论或互动，加入队列
            return (item.showIntro || item.showScene) ? item : null;
        })
        .filter(item => item !== null);
    
    // 如果有内容队列，开始播放
    if (gameState.courseContentQueue.length > 0) {
        showNextCourseContent();
        return;
    }
    
    // 都没有，直接进入结算
    proceedWithWeekSummary();
}

// 播放下一门课程的内容（导论 → 互动）
function showNextCourseContent() {
    if (gameState.courseContentQueue.length === 0) {
        // 所有内容播放完毕，进入结算
        proceedWithWeekSummary();
        return;
    }
    
    const item = gameState.courseContentQueue[0];
    
    // 如果这门课需要显示导论，先显示导论
    if (item.showIntro) {
        showCourseIntroModal(item.name, courseIntroductions[item.id], item);
        return;
    }
    
    // 如果不需要导论但需要互动，直接显示互动
    if (item.showScene) {
        showCourseSceneModal(item.name, courseScenes[item.id], item);
        return;
    }
    
    // 都不需要，跳过这门课
    gameState.courseContentQueue.shift();
    showNextCourseContent();
}

// 导论播放完成后的回调
function onCourseIntroComplete(item) {
    // 标记导论已播放
    gameState.shownCourseIntroIds[item.id] = true;
    
    // 如果这门课还需要显示互动，立即显示互动
    if (item.showScene) {
        showCourseSceneModal(item.name, courseScenes[item.id], item);
    } else {
        // 不需要互动，继续下一门课
        gameState.courseContentQueue.shift();
        showNextCourseContent();
    }
}

// 互动播放完成后的回调
function onCourseSceneComplete(item) {
    // 标记互动已播放
    gameState.shownCourseSceneIds[item.id] = true;
    
    // 继续下一门课
    gameState.courseContentQueue.shift();
    showNextCourseContent();
}
```

### 7.2 周结算后处理期末考试

```javascript
function proceedWithWeekSummary() {
    // ... 现有结算逻辑 ...
    
    // 结算后，检查是否有课程完成，需要显示期末考试
    const completedCourses = courses.filter(course => {
        const requiredTimes = course.requiredTimes || 1;
        const currentProgress = gameState.courseProgress[course.id] || 0;
        return currentProgress >= requiredTimes;
    });
    
    // 构建测验队列（只包含有测验题目的课程）
    gameState.quizQueue = completedCourses
        .filter(course => {
            const schedule = courseContentSchedule[course.id];
            const requiredTimes = course.requiredTimes || 1;
            const currentProgress = gameState.courseProgress[course.id] || 0;
            // 检查是否应该在这次显示考试
            return schedule && schedule.quiz === currentProgress && 
                   courseQuizzes[course.id] && courseQuizzes[course.id].length > 0;
        })
        .map(course => ({ id: course.id, name: course.name, type: course.type }));
    
    // 如果有测验队列，先进行测验
    if (gameState.quizQueue.length > 0) {
        // ... 现有测验逻辑 ...
    }
    
    // ... 显示周总结 ...
}
```

---

## 八、内容统计总结

### 8.1 有导论的课程（15门）
p1, p2, p3, p4, p5, p6, p7, p8, p10, p11, p12, p14, m1, m2, m3, l1, l2

### 8.2 有课堂互动的课程（10门）
p1（多段）, p2（多段+选择题）, p5（多段+选择题）, p6, p7, p10（多段）, p14, m1, m2

### 8.3 有考试题目的课程（11门）
p6（3道）, p7（3道）, p8（4道）, p10（4道）, p11（2道）, p12（5道）, p14（5道）, m1（5道）, m2（5道）, m3（5道）

### 8.4 无内容的课程（需要补充或跳过）
p9（中国问题）, p13（战区政治工作）, mi1（军事训练）, mi2（军事生活管理）, mi3（步兵战术）, mi4（游击战争）, mi5（特种战斗教育）

---

## 九、总结

本分配方案根据**课程游戏题目.md的实际内容**，合理分配了：
- **课程导论**：第1次上课时出现（首次了解）
- **课堂互动**：第2次或中间某次上课时出现（深入体验，仅一次）
- **期末考试**：最后一次上课时出现（检验成果）

这样设计的好处：
1. ✅ **符合学习规律**：先了解背景，再深入体验，最后检验成果
2. ✅ **避免重复**：导论和互动都只出现一次
3. ✅ **灵活适配**：根据课程的学习次数和实际内容自动调整
4. ✅ **体验流畅**：内容分散在不同学习次数，不会一次性信息过载
5. ✅ **内容完整**：根据题目文件实际内容分配，确保每门课程的内容都能正确展示

**注意事项**：
- p1、p2、p5 有丰富的课堂互动但无考试题目，这是正常的（互动内容已经足够丰富）
- p3、p4 只有导论，无互动和考试，这也是正常的（这些课程可能更注重理论介绍）
- 部分课程（p9、p13、军事课程）在题目文件中无内容，需要后续补充或跳过
