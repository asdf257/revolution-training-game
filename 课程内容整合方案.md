# 课程内容整合方案

## 一、内容分类与出现时机

### 1.1 课程导论（Course Introduction）
**出现时机**：**第1次选择该课程时**，在每日课程页面中，按周一到周五顺序显示
**出现方式**：弹窗展示课程导论文本
**目的**：让玩家了解课程背景、学习目标和历史意义

**流程位置**：
```
选课 → 确认本周课程 → 进入每日课程页面 → 显示课程安排预览 → 周一课程导论 → 互动 → 测验（如果完成） → 下一天...
```

### 1.2 课堂互动（Course Scene）
**出现时机**：**根据课程学习次数分配**（仅出现一次），在课程导论之后立即显示
- requiredTimes = 1：第1次上课时（与导论一起）
- requiredTimes = 2：第1次或第2次上课时（根据分配）
- requiredTimes = 3：第2次上课时
**出现方式**：剧情弹窗，包含场景描述、老师台词、A/B选项、结果文本
**目的**：增强沉浸感，让玩家体验课堂氛围，通过选择获得少量属性加成

**流程位置**：
```
每日课程页面 → 某天课程导论 → [课堂互动弹窗]（立即显示） → 测验（如果完成） → 下一天
```

### 1.3 考试题目（Course Quiz）
**出现时机**：**每节课播放完导论+互动后，如果该课程本次学习后完成requiredTimes，立即显示测验**
**出现方式**：测验弹窗（复用现有quiz-modal）
**目的**：检验学习成果，答对给予属性加成

**流程位置**：
```
每日课程页面 → 某天课程导论 → 互动 → [测验弹窗]（如果完成requiredTimes） → 下一天
```

**重要**：测验随每节课进行，周数值结算之后不再有测验队列

---

## 二、完整游戏流程（整合后）

### 2.1 每周完整流程

```mermaid
flowchart TD
    A[用户选择课程] --> B[点击确认本周课程]
    B --> C[进入"每日课程"页面]
    C --> D[显示"这周的课程要开始了"提示]
    D --> E[显示本周课程安排预览<br/>覆盖在主界面上]
    E --> F[显示"周一"标识]
    F --> G{第1门课是否存在?}
    G -->|是| H[显示第1门课的导论]
    H --> I{这门课有互动?}
    I -->|是| J[显示第1门课的互动]
    I -->|否| K{本次学习后完成requiredTimes?}
    J --> L[用户选择A/B]
    L --> M[显示结果文本]
    M --> K
    K -->|是| N[立即显示该课程的测验弹窗]
    K -->|否| O[点击"下一天"按钮]
    N --> P[测验完成]
    P --> O
    G -->|否| Q[显示"你有自己的安排，没有在学校活动"<br/>同一页面文本提示]
    Q --> O
    O --> R[显示"周二"标识]
    R --> S{第2门课是否存在?}
    S -->|是| T[显示第2门课的导论+互动]
    S -->|否| Q
    T --> U{本次学习后完成requiredTimes?}
    U -->|是| N
    U -->|否| V[点击"下一天"]
    V --> W[周三课程...]
    W --> X[周四课程...]
    X --> Y[周五课程...]
    Y --> Z[所有课程播放完毕]
    Z --> AA[统一进行周结算<br/>计算属性变化、健康值变化等]
    AA --> AB[显示完整周总结弹窗<br/>包含所有课程的属性变化、健康值变化等]
    AB --> AC{健康值<30?}
    AC -->|是| AD[医院剧情]
    AC -->|否| AE{有随机事件?}
    AE -->|是| AF[随机事件弹窗]
    AE -->|否| AG[进入下一周]
    AD --> AG
    AF --> AG
```

### 2.2 详细时序说明

#### 阶段一：进入"每日课程"页面
- **触发时机**：确认本周课程后
- **页面内容**：
  - 显示"这周的课程要开始了"提示
  - 显示本周课程安排预览（使用课程简介，覆盖在主界面上）
  - 显示"周一"标识

#### 阶段二：按周一到周五顺序播放课程
- **播放方式**：每天显示一门课的内容
- **播放顺序**：按 `selectedItems` 中课程顺序对应周一到周五
  - **周一**：第1门课（selectedItems[0]）
  - **周二**：第2门课（selectedItems[1]）
  - **周三**：第3门课（selectedItems[2]）
  - **周四**：第4门课（selectedItems[3]）
  - **周五**：第5门课（selectedItems[4]）
- **每天流程**：
  1. 显示"周X"标识
  2. 如果该天有课程：
     - 显示课程导论（如果有且需要显示）
     - 显示课程互动（如果有且需要显示）
     - 如果本次学习后完成 requiredTimes，立即显示该课程的测验弹窗
     - 点击"下一天"按钮进入下一天
  3. 如果该天无课程：
     - 显示"你有自己的安排，没有在学校活动"（同一页面文本提示）
     - 点击"下一天"按钮进入下一天

#### 阶段三：统一周结算
- **触发时机**：所有课程播放完毕后（周五结束后）
- **执行内容**：
  - 计算所有课程的属性加成（受学习效率影响）
  - 消耗健康值
  - 更新课程进度
  - 应用课堂互动的属性加成（如果有）
  - **注意**：周数值结算之后不再有测验队列（测验已随每节课进行）

#### 阶段四：完整周总结
- **显示方式**：弹窗形式
- **显示内容**：
  - 本周所有课程的属性变化
  - 健康值变化
  - 课程列表和描述
  - 与现有周总结一致的内容

---

## 三、内容分配方案

### 3.1 分配原则

根据每门课程的 `requiredTimes`（需要学习次数），合理分配：
- **课程导论**：第1次上课时出现
- **课堂互动**：第2次或中间某次上课时出现（仅出现一次）
- **期末考试**：最后一次上课时出现（完成 requiredTimes 后）

详细分配方案请参考：`课程内容分配方案.md`

### 3.2 分配示例

| requiredTimes | 第1次 | 第2次 | 第3次 | 示例课程 |
|--------------|-------|-------|-------|---------|
| 1 | 导论 | - | - | p3, p4（只有导论） |
| 1 | 导论 + 期末考试 | - | - | p8, p11, p12, m3（有导论和考试） |
| 1 | 导论 + 互动 + 期末考试 | - | - | m2（单次课程但内容丰富） |
| 2 | 导论 | 互动 | - | p1, p2, p5（有导论和互动，无考试） |
| 2 | 导论 + 互动 | 期末考试 | - | p6, p7, m1（第1次导论+互动，第2次考试） |
| 2 | 导论 | 互动 + 期末考试 | - | p14（第1次导论，第2次互动+考试） |
| 3 | 导论 | 互动 | 期末考试 | p10 |

---

## 四、数据结构设计

### 4.1 课程内容分配配置：`courseContentSchedule`

```javascript
// 课程内容分配配置（根据课程内容分配方案.md）
// intro: 第几次显示导论, scene: 第几次显示互动, quiz: 第几次显示考试
const courseContentSchedule = {
    // requiredTimes = 1 的课程
    p3: { intro: 1, scene: null, quiz: null },  // 只有导论，无互动和考试
    p4: { intro: 1, scene: null, quiz: null },  // 只有导论，无互动和考试
    p8: { intro: 1, scene: null, quiz: 1 },     // 有导论和考试（4道题），无互动
    p11: { intro: 1, scene: null, quiz: 1 },     // 有导论和考试（2道题），无互动
    p12: { intro: 1, scene: null, quiz: 1 },    // 有导论和考试（5道题），无互动
    m2: { intro: 1, scene: 1, quiz: 1 },        // 有导论、互动和考试（5道题）
    m3: { intro: 1, scene: null, quiz: 1 },     // 有导论和考试（5道题），无互动
    l1: { intro: 1, scene: null, quiz: null },  // 劳动课程，只有导论
    l2: { intro: 1, scene: null, quiz: null },   // 劳动课程，只有导论
    
    // requiredTimes = 2 的课程
    p1: { intro: 1, scene: 2, quiz: null },     // 有导论和互动（多段），无考试题目
    p2: { intro: 1, scene: 2, quiz: null },     // 有导论和互动（多段+选择题），无考试题目
    p5: { intro: 1, scene: 2, quiz: null },     // 有导论和互动（多段+选择题），无考试题目
    p6: { intro: 1, scene: 1, quiz: 2 },         // 第1次导论+互动，第2次考试（3道题）
    p7: { intro: 1, scene: 1, quiz: 2 },         // 第1次导论+互动，第2次考试（3道题）
    p14: { intro: 1, scene: 2, quiz: 2 },       // 第1次导论，第2次互动+考试（5道题）
    m1: { intro: 1, scene: 1, quiz: 2 },        // 第1次导论+互动，第2次考试（5道题）
    mi2: { intro: null, scene: null, quiz: null }, // 题目文件中无内容
    mi5: { intro: null, scene: null, quiz: null }, // 题目文件中无内容
    
    // requiredTimes = 3 的课程
    p10: { intro: 1, scene: 2, quiz: 3 },       // 有导论、互动（多段）和考试（4道题）
    mi1: { intro: null, scene: null, quiz: null }, // 题目文件中无内容
    mi3: { intro: null, scene: null, quiz: null }, // 题目文件中无内容
    mi4: { intro: null, scene: null, quiz: null }  // 题目文件中无内容
};
```

### 4.2 课程导论数据：`courseIntroductions`

**重要**：课程导论文字内容需要从 `课程游戏题目.md` 中提取，不是使用现有的课程描述。

根据 `课程游戏题目.md` 的结构，每门课程的导论内容在：
- 课程标题后的第一段或"课程导论"部分
- 例如：p6政治经济学有明确的"课程导论"部分
- 例如：p7辩证唯物主义有"课程导论"部分
- 例如：p14党课有"一、课程导论"部分

```javascript
// 课程导论数据（需要从课程游戏题目.md中提取）
const courseIntroductions = {
    p1: "从阶级视角出发，系统讲解社会结构、生产力与生产关系的基本原理，使学员理解社会发展的历史规律，为认识中国社会的性质与革命任务奠定基础。\n\n" +
        "\"一门是'社会科学概论'，或叫政治常识，由李凡夫同志主讲，包括'社会发展史''政治经济学'等课程，讲授马列主义关于社会发展规律的基本知识...\"",
    
    p2: "回顾自鸦片战争以来中国人民反帝反封建的英勇斗争，重点讲述中国共产党领导的新民主主义革命历程，从中汲取坚定的革命信仰与斗争智慧。\n\n" +
        "\"'中国革命运动史'这门课由何干之同志主讲，后来何定华同志也讲过。从鸦片战争、太平天国、戊戌政变、八国联军、辛亥革命、五四运动、北伐战争。土地革命一直讲到抗日战争...\"",
    
    p3: "紧密结合抗日战争形势与党的最新政策，分析国内外政治动态，培养学员的政治敏锐性与理论联系实际的能力。\n\n" +
        "\"陕北公学的政治教育特别值得介绍的是我们经常请到党中央领导同志来讲课，这是陕北公学在延安时得天独厚的优越条件...\"",
    
    p4: "深入剖析孙中山三民主义的思想内涵及其在旧民主主义革命时期的实践，通过历史比较，深刻认识其时代局限性与新民主主义革命的必然性。\n\n" +
        "三民主义作为孙中山先生提出的民主革命纲领，其思想内涵经历了重要发展...",
    
    p5: "阐述中国共产党在民族危亡关头倡导并建立统一战线的战略思想，学习团结一切抗日力量的方针政策，理解抗日民族统一战线在抗日战争中的深刻意义。\n\n" +
        "\"第二门课是'抗日民族统一战线'，由何干之同志主讲，邵式平同志也讲过。主要讲抗日民族统一战线的提出、产生的历史背景和在实践中的重要作用...\"",
    
    p6: "陕北公学汇聚了来自工农群众、知识分子等不同群体的进步青年，他们虽怀揣革命热忱，但对社会经济本质、阶级剥削根源的认知存在明显差异——部分学员有基层生产生活经验，却缺乏系统经济理论支撑；少数青年接触过西方经济学说，尚未建立科学的经济认知。为帮助学员筑牢马克思主义经济理论根基，认清中国半殖民地半封建经济的症结，培育能服务根据地经济建设、适配抗战物资保障需求的革命骨干，陕北公学专门开设政治经济学课程，开展系统化的马克思主义经济理论教学。\n\n" +
        "政治经济学课程以马克思主义政治经济学基本原理为核心，重点讲解商品、货币、资本的本质，分析资本主义经济的内在矛盾与发展规律，揭露阶级剥削的经济根源，同时结合中国实际剖析封建经济残余与帝国主义经济侵略对中国社会的压迫，解读抗日根据地经济建设的基本原则与实践路径。课程教学坚持理论联系实际，既讲授经典经济理论，又紧扣抗日战争时期的经济任务，帮助学员理解经济工作与革命战争、群众生活的密切关联，掌握根据地经济建设、物资保障的基本思路。通过系统学习，学员能够认清不同阶级的经济地位与利益诉求，提升运用经济理论解决革命实际问题的能力，为后续参与抗日根据地建设、开展群众经济工作奠定理论基础。\n\n" +
        "\"'政治经济学'这门课，李凡夫、徐冰同志都讲过。高级班还讲过《资本论》第一卷解说。此外，艾思奇、何干之、李凡夫等十来位同志集体编写了一本《社会科学概论》，印发给学员阅读。\"",
    
    p7: "抗战时期的进步青年虽心怀救国理想，却受限于成长环境，思想认知呈现多元态势：有的受传统唯心主义思想影响，有的被形而上学思维束缚，还有的因缺乏科学方法论，难以理性分析革命形势的复杂变化。为帮助学员破除错误思想桎梏，树立辩证唯物主义的世界观与方法论，提升研判局势、解决革命实际问题的能力，陕北公学开设辩证唯物主义课程，为学员开展系统的马克思主义哲学启蒙与教育。\n\n" +
        "辩证唯物主义课程核心是传授马克思主义哲学的基本原理，重点讲解物质与意识的辩证关系、对立统一规律、质量互变规律、否定之否定规律等，引导学员坚持一切从实际出发，反对主观主义、教条主义。课程教学注重理论联系革命实际，既阐释哲学基本概念与原理，又结合抗日战争的复杂形势、中国革命的具体实践，分析革命过程中遇到的矛盾与问题，帮助学员学会用辩证思维看待敌我关系、抗战形势的发展变化。通过系统学习，学员逐步摒弃错误思维方式，掌握科学的认知方法与工作方法，增强革命实践的主动性与科学性，为投身抗日救亡运动、开展革命工作提供坚实的思想武器。\n\n" +
        "\"'辩证唯物主义'这门课，先后由艾思奇、陈唯实主讲。\"",
    
    p8: "陕北公学的学员多聚焦于国内抗日救亡运动，对世界无产阶级革命的历史进程、发展规律了解较为零散，尤其对俄国十月革命的经验、国际共产主义运动的潮流认知不深，难以明晰中国革命在世界革命格局中的定位。为拓宽学员国际视野，帮助其汲取世界革命经验、坚定中国革命必胜信念，培育兼具家国情怀与国际视野的革命骨干，陕北公学开设世界革命运动史课程，开展系统的革命历史熏陶与理论引导。\n\n" +
        "世界革命运动史课程主要梳理世界无产阶级革命运动的兴起与发展历程，重点讲解巴黎公社、俄国十月革命等重大革命事件的历史意义，分析不同国家革命运动的特点与经验教训，阐释无产阶级在世界革命中的领导作用。课程教学注重衔接中国革命实际，既介绍世界革命的历史进程，又结合中国抗日战争与新民主主义革命的实践，帮助学员理解中国革命与世界革命的内在联系，明确中国革命的国际意义。通过系统学习，学员进一步坚定革命信念，拓宽理论视野，为投身中国革命、呼应世界革命潮流奠定思想与认知基础。",
    
    p10: "作为革命干部培养的核心阵地，陕北公学学员的革命基础参差不齐：部分党员学员具备一定的马克思列宁主义理论储备，却缺乏系统梳理；多数进步青年怀揣革命热情，对经典理论的核心要义、实践价值理解浅显，甚至存在认知偏差。为统一学员思想、夯实革命理论根基，打造坚定的马克思主义革命者队伍，陕北公学将马克思列宁主义课程列为核心必修，为学员开展全方位、系统化的经典理论教育。\n\n" +
        "马克思列宁主义课程核心是传授马克思主义的基本立场、观点与方法，重点讲解马克思主义的哲学、政治经济学、科学社会主义基本原理，以及列宁主义关于无产阶级革命、无产阶级专政的重要理论，解读马克思列宁主义的革命性与科学性。课程教学坚持理论联系实际，既阐释经典理论内涵，又结合抗日战争的现实任务与中国革命的具体实践，帮助学员理解马克思列宁主义对中国革命的指导意义，学会用经典理论解决革命实际问题。通过系统学习，学员逐步深化对马克思列宁主义的理解与认同，坚定革命理想信念，为投身革命事业、践行马克思列宁主义的革命主张提供坚实的理论支撑。\n\n" +
        "\"'马列主义'这门课由李凡夫、李培之同志讲。后来由何于之同志主讲。主要讲《共产党宣言》、《马克思主义三个来源和三个组成部分》、《社会主义从空想到科学的发展》等著作，要求学员直接读原著...\"",
    
    p11: "面对民族危亡与革命洪流，陕北公学学员虽立志投身救国事业，却对"社会主义"的科学内涵、发展路径认知模糊，部分青年混淆了空想社会主义与科学社会主义的界限，对中国革命的前进方向、最终目标缺乏清晰认知。为帮助学员厘清思想误区、明确革命航向，坚定共产主义理想信念，培育投身社会主义革命与建设的骨干力量，陕北公学开设科学社会主义课程，开展针对性的理论讲解与思想引导。\n\n" +
        "科学社会主义课程主要讲解社会主义从空想到科学的发展历程，重点阐释科学社会主义的基本原理，分析无产阶级解放运动的道路与目标，介绍无产阶级专政、社会主义建设的基本理论，结合中国实际解读新民主主义革命与科学社会主义的内在关联。课程教学注重理论联系实际，既梳理科学社会主义的理论脉络，又结合抗日战争的现实与中国革命的实践，帮助学员理解中国共产党领导的革命事业与科学社会主义的一致性，明确中国革命最终走向社会主义的历史必然性。通过系统学习，学员进一步坚定共产主义理想信念，明确革命奋斗目标，为投身中国革命、推动社会主义理想的实现奠定思想基础。",
    
    p12: "本课程旨在帮助学生了解世界主要国家的政治、经济与军事形势，分析国际关系的发展动态，并培养学生的国际视野和革命判断能力。在全球反法西斯战争形势下，课程特别关注日本侵略扩张、欧洲战争局势、苏联和美国等大国的外交政策，以及殖民地和被压迫民族的反抗运动。\n\n" +
        "当时陕北公学经常请到党中央领导同志来讲课，这是陕北公学在延安时得天独厚的优越条件。毛主席特别关心陕公的教学工作，他规定政治局委员都要来讲课，他自己第一个带头来讲，主要是讲时事和形势。每逢国内外形势发生重大变化或发生重大政治事件时，毛主席和中央领导同志就到陕北公学来做报告，帮助学员及时掌握阶级斗争和民族斗争。\n\n" +
        "通过课堂讲授、时事分析和资料阅读，使学生能够掌握世界主要力量对抗法西斯侵略的基本态势，理解国内革命与国际形势之间的联系，培养学生批判性思维与社会责任感，为今后投身抗日救国和革命建设打下思想基础。",
    
    p14: "陕北公学的学员来自全国各地，政治面貌和思想基础并不一致。部分学员在入校前已是中共党员或共青团员，也有相当数量的进步青年尚未正式加入党组织。为了统一思想、提高政治觉悟，并为革命事业培养可靠的骨干力量，陕北公学在各类政治课程之外，有计划地开设党课，对学员进行系统的党的基本知识教育。\n\n" +
        "党课主要是对学员进行党的基本知识教育，向学员介绍党的纲领、章程、历史与优良传统等，强化学员党性修养。党课教学注重理论联系实际，既讲党的基本理论，又结合抗日战争的现实任务，帮助学员理解中国共产党在抗日战争中发挥的重要作用。通过系统学习，进一步推动学员在思想上向党组织靠拢，为发展党员和加强党组织建设奠定思想基础。",
    
    m1: "这门课有计划地组织学员参加建校劳动和农业生产劳动，通过劳动学习生产知识，培养劳动人民的思想感情，改掉学员的学生腔。",
    
    m2: "讲民众运动的理论和经验，教育学员了解民众运动在中国革命和抗日战争中的意义和作用，如何动员、组织、训练、领导广大群众参加抗日战争，掌握党在民众运动中对各阶层的政策等等。",
    
    m3: "抗日战争不仅是一场军事斗争，更是一场深刻的政治斗争。在敌后抗日根据地中，能否建立和巩固人民政权，直接关系到抗战的持久性和根据地的生存发展。因此，对陕北公学学员而言，掌握抗日根据地政权建设的理论与实践，是走向实际工作岗位前必须完成的重要学习任务。\n\n" +
        "如何在坚持党的领导的前提下，实行广泛民主，吸收各阶层抗日力量参与政权建设，是当时必须回答的重要问题。本课程系统介绍抗日战争时期敌后抗日根据地政权建设的基本理论和实践经验，重点讲授陕甘宁边区民主选举制度的形成过程、原则和具体做法等。同时，通过大量实际案例，使学员理解基层政府在组织群众、动员抗战、贯彻统一战线政策中的重要作用。\n\n" +
        "通过本课程的学习，使学员在思想上认识到民主政治是抗日根据地政权的生命线，在实践上初步掌握开展基层政权工作的基本方法，为今后承担政府工作或配合政权建设奠定基础。\n\n" +
        "讲如何建立敌后抗日根据地政权的理论和政策，教育学员了解根据地实行民主制度和政权建设的重要性，以及如何领导和帮助根据地群众建立抗日民主政权等等。",
    
    l1: "组织学员参与校园建设、营房修筑、防御工事构建等体力劳动，掌握基本的生产建设技能，培育自力更生、艰苦奋斗的精神。",
    
    l2: "组织学员开荒种地、兴修水利、作物栽培与收割，亲身体验农业生产的全过程，深刻理解'自己动手、丰衣足食'对于坚持长期抗战的重大意义。"
};
```

**注意**：以上内容需要根据 `课程游戏题目.md` 的实际内容进行提取和整理，确保导论文本准确完整。

### 4.3 课堂互动数据：`courseScenes`

```javascript
const courseScenes = {
    p1: {
        title: "社会科学概论",
        intro: "窑洞内土墙斑驳，晨光从纸窗斜照进来，木桌板凳简陋却整齐。\n李凡夫老师手持粉笔，正在为大家讲解社会理论...",
        teacherIntro: "李凡夫老师长期从事党的文化教育工作，被公认为陕北公学的「名教授」之一...",
        teacherLine: "同志们，马克思在《政治经济学批判》序言中曾说：「当社会的物质生产力发展到一定阶段...」",
        choices: [
            {
                text: "我高高举起双手，准备回答",
                result: "我高高举起双手：「老师，我知道！马克思指出生产力与生产关系的矛盾运动...」",
                effect: { political: 1 }  // 可选属性加成
            },
            {
                text: "我低下头，准备倾听别人的回答",
                result: "课堂安静片刻。一名戴眼镜的男学员举手：「老师，我知道！...」"
            }
        ],
        ending: "听完回答，李老师点点头，赞许地看着我们：「很好！这正是历史唯物主义的基石...」"
    },
    // ... 其他课程
};
```

### 4.4 游戏状态扩展

```javascript
gameState = {
    // ... 现有字段
    courseProgress: {},           // 记录每门课程的学习进度 { courseId: progress }
    shownCourseIntroIds: {},      // 记录每门课程是否已播放导论 { courseId: true/false }
    shownCourseSceneIds: {},      // 记录每门课程是否已播放互动 { courseId: true/false }
    
    // 每日课程页面状态
    currentDay: 1,                // 当前日期（1=周一, 2=周二, ..., 5=周五）
    dailyCourses: [],             // 本周的课程列表（最多5门，按selectedItems顺序）
    courseSceneEffects: {}        // 课堂互动的属性加成（在周结算时统一应用）
};
```

---

## 五、实现要点

### 5.1 `confirmWeek()` 函数改造

```javascript
function confirmWeek() {
    if (gameState.selectedItems.length === 0) return;
    
    // 进入"每日课程"页面
    enterDailyCoursePage();
}

// 进入"每日课程"页面
function enterDailyCoursePage() {
    // 隐藏主游戏界面
    document.getElementById('gameContainer').style.display = 'none';
    
    // 显示"每日课程"页面
    document.getElementById('dailyCoursePage').style.display = 'block';
    
    // 初始化每日课程状态
    gameState.currentDay = 1; // 1=周一, 2=周二, ..., 5=周五
    gameState.dailyCourses = gameState.selectedItems.slice(0, 5); // 最多5门课
    
    // 显示"这周的课程要开始了"提示
    showWeekStartPrompt();
    
    // 显示本周课程安排预览
    showCourseSchedulePreview();
    
    // 开始显示周一的课程
    showDayCourse(1);
}

// 显示"这周的课程要开始了"提示
function showWeekStartPrompt() {
    // 显示提示文本
    document.getElementById('weekStartPrompt').textContent = '这周的课程要开始了';
    document.getElementById('weekStartPrompt').style.display = 'block';
}

// 显示本周课程安排预览（只显示课程名称列表，覆盖在主界面上）
function showCourseSchedulePreview() {
    const previewContainer = document.getElementById('courseSchedulePreview');
    previewContainer.innerHTML = '';
    
    gameState.dailyCourses.forEach((course, index) => {
        const dayNames = ['周一', '周二', '周三', '周四', '周五'];
        const dayDiv = document.createElement('div');
        dayDiv.className = 'schedule-course-item';
        dayDiv.innerHTML = `
            <div class="schedule-day">${dayNames[index]}</div>
            <div class="schedule-course-name">${course.name}</div>
        `;
        previewContainer.appendChild(dayDiv);
    });
    
    // 如果未选满5门，显示"你有自己的安排"
    for (let i = gameState.dailyCourses.length; i < 5; i++) {
        const dayNames = ['周一', '周二', '周三', '周四', '周五'];
        const dayDiv = document.createElement('div');
        dayDiv.className = 'schedule-course-item empty';
        dayDiv.innerHTML = `
            <div class="schedule-day">${dayNames[i]}</div>
            <div class="schedule-course-name">你有自己的安排</div>
        `;
        previewContainer.appendChild(dayDiv);
    }
}

// 显示某一天的课程
function showDayCourse(day) {
    const dayNames = ['周一', '周二', '周三', '周四', '周五'];
    const dayIndex = day - 1;
    
    // 更新当前日期显示
    document.getElementById('currentDayLabel').textContent = dayNames[dayIndex];
    
    // 隐藏课程安排预览
    document.getElementById('courseSchedulePreview').style.display = 'none';
    
    const course = gameState.dailyCourses[dayIndex];
    
    if (!course) {
        // 该天没有课程，显示"你有自己的安排"
        showNoCourseMessage(day);
        return;
    }
    
    // 该天有课程，显示课程内容
    const currentProgress = gameState.courseProgress[course.id] || 0;
    const schedule = courseContentSchedule[course.id];
    
    if (!schedule) {
        // 无内容配置，直接进入下一天
        proceedToNextDay();
        return;
    }
    
    // 检查是否需要显示导论
    const showIntro = schedule.intro === currentProgress + 1 && 
                     !gameState.shownCourseIntroIds[course.id];
    
    // 检查是否需要显示互动
    const showScene = schedule.scene === currentProgress + 1 && 
                     !gameState.shownCourseSceneIds[course.id];
    
    if (showIntro) {
        // 显示导论
        showCourseIntroModal(course.name, courseIntroductions[course.id], { 
            course: course, 
            day: day,
            showScene: showScene 
        });
    } else if (showScene) {
        // 直接显示互动
        showCourseSceneModal(course.name, courseScenes[course.id], { 
            course: course, 
            day: day 
        });
    } else {
        // 都不需要，直接进入下一天
        proceedToNextDay();
    }
}

// 显示"你有自己的安排"消息
function showNoCourseMessage(day) {
    document.getElementById('noCourseMessage').textContent = '你有自己的安排，没有在学校活动';
    document.getElementById('noCourseMessage').style.display = 'block';
    document.getElementById('nextDayBtn').style.display = 'block';
}

// 进入下一天
function proceedToNextDay() {
    gameState.currentDay++;
    
    if (gameState.currentDay > 5) {
        // 所有课程播放完毕，进行统一结算
        proceedWithWeekSummary();
    } else {
        // 显示下一天的课程
        showDayCourse(gameState.currentDay);
    }
}

### 5.2 课程导论弹窗函数

```javascript
function showCourseIntroModal(courseName, introText, context) {
    // 显示导论弹窗，包含课程名称和导论文本
    // context: { course, day, showScene }
    // 点击"开始学习"后调用 onCourseIntroComplete(context)
}

function onCourseIntroComplete(context) {
    const { course, day, showScene } = context;
    
    // 标记导论已播放
    gameState.shownCourseIntroIds[course.id] = true;
    
    // 如果这门课还需要显示互动，立即显示互动
    if (showScene) {
        showCourseSceneModal(course.name, courseScenes[course.id], { course, day });
    } else {
        // 不需要互动，检查是否需要测验，然后进入下一天
        checkAndShowQuiz(course, day);
    }
}
```

### 5.3 课堂互动弹窗函数

```javascript
function showCourseSceneModal(courseName, scene, context) {
    // 显示弹窗，包含：
    // 1. 场景描述（intro）
    // 2. 老师介绍（teacherIntro）
    // 3. 老师台词（teacherLine）
    // 4. A/B选项按钮
    // 5. 选择后显示结果（result）和收尾（ending）
    // 6. 应用属性加成（如果有effect）
    // 7. "继续"按钮，点击后调用 onCourseSceneComplete(context)
    // context: { course, day }
}

function onCourseSceneComplete(context) {
    const { course, day } = context;
    
    // 标记互动已播放
    gameState.shownCourseSceneIds[course.id] = true;
    
    // 检查是否需要测验，然后进入下一天
    checkAndShowQuiz(course, day);
}

// 检查并显示测验（如果本次学习后完成requiredTimes）
function checkAndShowQuiz(course, day) {
    const requiredTimes = course.requiredTimes || 1;
    const currentProgress = gameState.courseProgress[course.id] || 0;
    const newProgress = currentProgress + 1;
    
    // 检查是否完成requiredTimes
    if (newProgress >= requiredTimes) {
        const schedule = courseContentSchedule[course.id];
        // 检查是否有测验题目
        if (schedule && schedule.quiz === newProgress && 
            courseQuizzes[course.id] && courseQuizzes[course.id].length > 0) {
            // 立即显示该课程的测验弹窗
            showCourseQuiz(course, () => {
                // 测验完成后，更新进度并进入下一天
                gameState.courseProgress[course.id] = newProgress;
                proceedToNextDay();
            });
            return;
        }
    }
    
    // 不需要测验，更新进度并进入下一天
    gameState.courseProgress[course.id] = newProgress;
    proceedToNextDay();
}
```

### 5.4 周结算后处理期末考试

```javascript
function proceedWithWeekSummary() {
    // ... 现有结算逻辑 ...
    
    // 结算后，检查是否有课程完成，需要显示期末考试
    const courses = gameState.selectedItems.filter(item => item.itemType === 'course');
    const completedCourses = courses.filter(course => {
        const requiredTimes = course.requiredTimes || 1;
        const currentProgress = gameState.courseProgress[course.id] || 0;
        return currentProgress >= requiredTimes;
    });
    
    // 构建测验队列（只包含有测验题目的课程，且应该在这次显示考试）
    gameState.quizQueue = completedCourses
        .filter(course => {
            const schedule = courseContentSchedule[course.id];
            const requiredTimes = course.requiredTimes || 1;
            const currentProgress = gameState.courseProgress[course.id] || 0;
            // 检查是否应该在这次显示考试
            return schedule && schedule.quiz === currentProgress && 
                   courseQuizzes[course.id] && courseQuizzes[course.id].length > 0;
        })
        .map(course => ({ id: course.id, name: course.name, type: course.type }));
    
    // 如果有测验队列，先进行测验
    if (gameState.quizQueue.length > 0) {
        // ... 现有测验逻辑 ...
    }
    
    // ... 显示周总结 ...
}
```

### 5.5 显示完整周总结

```javascript
function showWeekSummaryModal(oldStats, oldHealth) {
    const currentWeek = gameState.week;
    
    // 显示周总结弹窗（只显示数值变化）
    document.getElementById('summaryWeekBadge').textContent = `第 ${currentWeek} 周`;
    
    // 显示课程名称列表（不显示描述）
    const coursesContainer = document.getElementById('summaryCourses');
    coursesContainer.innerHTML = '';
    
    gameState.dailyCourses.forEach(item => {
        const div = document.createElement('div');
        div.className = `summary-course-item ${item.type || item.itemType}`;
        div.innerHTML = `
            <div class="summary-course-icon ${item.type || item.itemType}">${item.icon || '📚'}</div>
            <div class="summary-course-name">${item.name}</div>
        `;
        coursesContainer.appendChild(div);
    });
    
    // 只显示属性变化（数值变化）
    const statsContainer = document.getElementById('summaryStats');
    statsContainer.innerHTML = '';
    
    // 健康值变化
    const healthChange = gameState.health - oldHealth;
    const healthChangeClass = healthChange > 0 ? 'positive' : (healthChange < 0 ? 'negative' : '');
    const healthChangeText = healthChange > 0 ? '+' + healthChange : healthChange;
    const healthStat = document.createElement('div');
    healthStat.className = 'summary-stat health-stat';
    healthStat.innerHTML = `
        <div class="summary-stat-value">${gameState.health}</div>
        <div class="summary-stat-change ${healthChangeClass}">${healthChangeText}</div>
        <div class="summary-stat-name">💚 健康值</div>
    `;
    statsContainer.appendChild(healthStat);
    
    // 各属性变化
    Object.keys(gameState.stats).forEach(type => {
        const change = gameState.stats[type] - oldStats[type];
        if (change === 0) return; // 只显示有变化的属性
        
        const stat = document.createElement('div');
        stat.className = 'summary-stat';
        const changeClass = change > 0 ? 'positive' : 'negative';
        const changeText = change > 0 ? '+' + change : change;
        stat.innerHTML = `
            <div class="summary-stat-value">${gameState.stats[type]}</div>
            <div class="summary-stat-change ${changeClass}">${changeText}</div>
            <div class="summary-stat-name">${statNames[type]}</div>
        `;
        statsContainer.appendChild(stat);
    });
    
    document.getElementById('summaryModal').classList.add('show');
    
    // 更新UI
    updateUI();
    gameState.selectedItems = [];
    gameState.pendingRandomEvent = true;
    gameState.week++;
    updateUI();
}
```

---

## 六、课程内容映射表

| 题目文件课程 | 游戏课程ID | 课程导论 | 课堂互动 | 考试题目 |
|-------------|-----------|---------|---------|---------|
| 社会发展史 | p1 | ✅ | ✅（多段） | ❌ |
| 中国革命运动史 | p2 | ✅ | ✅（多段+选择题） | ❌ |
| 时事政治报告 | p3 | ✅ | ❌ | ❌ |
| 三民主义研究 | p4 | ✅ | ❌ | ❌ |
| 抗日民族统一战线 | p5 | ✅ | ✅（多段+选择题） | ❌ |
| 政治经济学 | p6 | ✅ | ✅ | ✅ |
| 辩证唯物主义 | p7 | ✅ | ✅ | ✅ |
| 世界革命运动史 | p8 | ✅ | ❌ | ✅ |
| 马克思列宁主义 | p10 | ✅ | ✅ | ✅ |
| 科学社会主义 | p11 | ✅ | ❌ | ✅ |
| 国外时事和形势 | p12 | ✅ | ❌ | ✅ |
| 党课 | p14 | ✅ | ✅ | ✅ |
| 劳动教育 | m1/l1/l2 | ✅ | ✅ | ✅ |
| 民众运动 | m2 | ✅ | ✅ | ✅ |
| 政府工作 | m3 | ✅ | ❌ | ✅ |

---

## 七、HTML/CSS 新增内容

### 6.1 课程导论弹窗

```html
<div class="course-intro-modal" id="courseIntroModal">
    <div class="course-intro-card">
        <div class="course-intro-header">
            <h2 class="course-intro-title" id="courseIntroTitle">课程导论</h2>
        </div>
        <div class="course-intro-content" id="courseIntroContent">
            <!-- 导论文本 -->
        </div>
        <button class="course-intro-btn" onclick="closeCourseIntro()">开始学习</button>
    </div>
</div>
```

### 6.2 课堂互动弹窗（复用或新建）

```html
<div class="course-scene-modal" id="courseSceneModal">
    <div class="course-scene-card">
        <div class="course-scene-header">
            <h2 class="course-scene-title" id="courseSceneTitle">课堂互动</h2>
        </div>
        <div class="course-scene-content" id="courseSceneContent">
            <!-- 场景描述、老师台词 -->
        </div>
        <div class="course-scene-choices" id="courseSceneChoices">
            <!-- A/B选项按钮 -->
        </div>
        <div class="course-scene-result" id="courseSceneResult" style="display: none;">
            <!-- 结果文本、收尾 -->
        </div>
        <button class="course-scene-btn" id="courseSceneContinueBtn" onclick="continueCourseScene()" style="display: none;">继续</button>
    </div>
</div>
```

---

## 八、实施优先级

### 阶段一：核心流程（必须）
1. ✅ 实现课程导论弹窗和播放逻辑
2. ✅ 实现课堂互动弹窗和播放逻辑
3. ✅ 改造 `confirmWeek()` 函数
4. ✅ 实现3-5门课程的导论和互动（p14、m2、m1、p6、p7）

### 阶段二：内容完善（重要）
5. ✅ 录入所有课程的导论文本
6. ✅ 录入所有有课堂互动的课程的剧情内容
7. ✅ 完善测验题目（补充缺失的题目）

### 阶段三：体验优化（可选）
8. ✅ 实现多段课堂互动（p1、p5、p2的多段选择）
9. ✅ 实现课堂互动中的选择题（嵌入到剧情中）
10. ✅ 优化弹窗动画和过渡效果
11. ✅ 添加音效和背景音乐

---

## 九、注意事项

1. **存档兼容性**：`shownCourseIntroIds`、`shownCourseSceneIds`、`courseProgress`、`currentDay` 需要随存档保存和恢复
2. **重复播放**：课程导论只播放一次（首次选择），课堂互动也只播放一次（按分配次数）
3. **属性加成**：课堂互动的属性加成应较小（+1或+2），避免影响游戏平衡
4. **文本长度**：导论和剧情文本可能较长，需要合适的滚动或分页显示
5. **每日课程页面**：确认本周课程后直接进入，同一页面切换内容，不需要返回主界面
6. **测验时机**：每节课播放完导论+互动后立即检查，如果完成requiredTimes则立即显示测验
7. **周结算时机**：所有课程播放完毕后统一结算，周数值结算之后不再有测验队列
8. **课程顺序**：按 `selectedItems` 的顺序对应周一到周五，未选满5门时从周五往前显示"你有自己的安排"

---

## 十、示例：完整一周的流程

### 示例：用户选择了3门课程（p14, m2, p6）

**确认本周课程后**：
1. 进入"每日课程"页面
2. 显示"这周的课程要开始了"提示
3. 显示课程安排预览（只显示课程名称列表）：
   ```
   周一：党课
   周二：民众运动
   周三：政治经济学
   周四：你有自己的安排
   周五：你有自己的安排
   ```

**周一（p14党课，第1次学习）**：
1. 显示"周一"标识
2. 显示课程导论弹窗（因为 `intro: 1 === 0 + 1`）
3. 点击"开始学习" → 不显示互动（因为 `scene: 2`，需要第2次）
4. 更新进度：`courseProgress.p14 = 1`
5. 不显示测验（因为未完成 requiredTimes）
6. 点击"下一天"按钮

**周二（m2民众运动，第1次学习）**：
1. 显示"周二"标识
2. 显示课程导论弹窗（因为 `intro: 1 === 0 + 1`）
3. 点击"开始学习" → 立即显示课堂互动弹窗（因为 `scene: 1 === 0 + 1`）
4. 用户选择A/B → 显示结果 → 点击"继续"
5. 更新进度：`courseProgress.m2 = 1`
6. **立即显示测验弹窗**（因为 `quiz: 1 === 1` 且完成 requiredTimes）
7. 测验完成 → 点击"下一天"按钮

**周三（p6政治经济学，第1次学习）**：
1. 显示"周三"标识
2. 显示课程导论弹窗（因为 `intro: 1 === 0 + 1`）
3. 点击"开始学习" → 立即显示课堂互动弹窗（因为 `scene: 1 === 0 + 1`）
4. 用户选择A/B → 显示结果 → 点击"继续"
5. 更新进度：`courseProgress.p6 = 1`
6. 不显示测验（因为未完成 requiredTimes）
7. 点击"下一天"按钮

**周四（无课程）**：
1. 显示"周四"标识
2. 显示"你有自己的安排，没有在学校活动"（同一页面文本提示）
3. 点击"下一天"按钮

**周五（无课程）**：
1. 显示"周五"标识
2. 显示"你有自己的安排，没有在学校活动"（同一页面文本提示）
3. 点击"下一天"按钮

**所有课程播放完毕后**：
1. 统一进行周结算：
   - 计算所有课程的属性加成
   - 消耗健康值
   - 应用课堂互动的属性加成
2. **不再有测验队列**（测验已随每节课进行）
3. 显示完整周总结弹窗（只显示数值变化）：
   - 课程名称列表（不显示描述）
   - 只显示有变化的属性数值变化
   - 健康值变化
4. 点击"继续下一周" → 进入下一周

---

## 十一、总结

本方案将课程游戏题目.md中的所有内容有机整合到游戏中，并根据课程学习次数合理分配：

- **课程导论**：第1次上课时展示，让玩家了解课程背景
- **课堂互动**：根据课程次数分配（仅出现一次），增强沉浸感和代入感
  - requiredTimes = 1 且有互动：第1次与导论一起（如 m2）
  - requiredTimes = 2 且有互动：第1次导论+互动（如 p6, p7, m1）或第2次互动（如 p1, p2, p5, p14）
  - requiredTimes = 3 且有互动：第2次互动（如 p10）
- **考试题目**：最后一次上课时展示，检验学习成果

### 分配优势：
1. ✅ **符合学习规律**：先了解背景，再深入体验，最后检验成果
2. ✅ **避免重复**：导论和互动都只出现一次，不会重复播放
3. ✅ **灵活适配**：根据课程的 `requiredTimes` 和实际内容自动调整分配
4. ✅ **体验流畅**：每门课的导论后立即播放该课的互动，内容连贯不打断
5. ✅ **内容完整**：根据题目文件实际内容分配，确保每门课程的内容都能正确展示
6. ✅ **每日流程**：按周一到周五顺序，每天显示一门课，增强时间感和沉浸感
7. ✅ **测验及时**：每节课完成后立即测验，周结算后不再有测验队列

### 特殊分配说明：
- **p6, p7, m1**：第1次同时显示导论和互动，第2次显示考试（内容集中在前半段）
- **p1, p2, p5**：第1次导论，第2次互动，无考试题目（互动内容丰富）
- **p14**：第1次导论，第2次互动+考试（标准分配）
- **m2**：第1次导论+互动+考试（单次课程但内容丰富）

通过合理的时序安排和数据结构设计，确保玩家能够：
1. 了解每门课程的学习目标和历史意义
2. 体验真实的课堂氛围和师生互动
3. 通过选择影响角色成长
4. 通过测验巩固学习成果

整个流程自然流畅，不会打断游戏节奏，反而能增强游戏的叙事性和教育意义。

**详细分配方案请参考：`课程内容分配方案.md`**

---

## 十二、课程导论内容提取说明

### 12.1 提取来源
所有课程导论文字内容需要从 `课程游戏题目.md` 中提取，**不是使用现有的课程描述**。

### 12.2 提取规则
1. **有明确"课程导论"部分的课程**：
   - p6（政治经济学）：有"六、政治经济学" → "课程导论"部分
   - p7（辩证唯物主义）：有"七、辩证唯物主义" → "课程导论"部分
   - p8（世界革命运动史）：有"八、世界革命运动史" → "课程导论"部分
   - p10（马克思列宁主义）：有"九、马克思列宁主义" → 第一段和第二段
   - p11（科学社会主义）：有"十、科学社会主义" → 第一段和第二段
   - p12（国外时事和形势）：有"国外时事和形势" → "一、课程导论"部分
   - p14（党课）：有"党课" → "一、课程导论"部分
   - m1（劳动教育）：有"劳动教育" → "一、课程导论"部分
   - m2（民众运动）：有"民众运动" → "一、课程导论"部分
   - m3（政府工作）：有"政府工作" → "一、课程导论"部分

2. **没有明确"课程导论"部分的课程**：
   - p1（社会发展史）：使用第一段描述 + 引用段落
   - p2（中国革命运动史）：使用第一段描述 + 引用段落
   - p3（时事政治报告）：使用第一段描述 + 引用段落
   - p4（三民主义研究）：使用第一段和第二段描述
   - p5（抗日民族统一战线）：使用第一段描述 + 引用段落

3. **劳动课程**：
   - l1、l2：使用简短的课程导论描述

### 12.3 提取注意事项
- 保留原文的段落结构和引用格式
- 包含历史背景和课程意义
- 确保文本完整，不遗漏重要信息
- 格式化为适合弹窗显示的文本（使用 `\n\n` 分隔段落）
